apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-deployment
  labels:
    app: deploy
spec:
  replicas: 3
  selector:
    matchLabels:
      app: deploy
  template:
    metadata:
      labels:
        app: deploy
    spec:
      containers:
      - name: webapp
        image: 460115791670.dkr.ecr.us-east-1.amazonaws.com/bkbiet-project:nginx_server
        # imagePullPolicy: Never
        ports:
        - containerPort: 80
